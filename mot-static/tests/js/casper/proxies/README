Casper is unable to load npm modules directly, so we use a proxy pattern
to load them like this.

proxies/lodash.js

module.exports = require('lodash');

casper/source.js

var _ = require('../proxies/lodash');