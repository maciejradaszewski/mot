<?php

/** @var \Dvsa\Mot\ApiClient\Resource\Item\MotTest $motTest */
$motTest = $this->motTest;

$brakeTestResult = $motTest->getBrakeTestResult();

?>

<div class="row" id="brakeResults">
    <div class="col-sm-4">
        <h2>Brake results overall</h2>
    </div>
    <div class="col-sm-8">
        <p class="primary-item">
            <?php
            if ($motTest->isTesterBrakePerformanceNotTested() == true) {
                $escLabel = 'Not tested';
            } elseif ($brakeTestResult == null) {
                $escLabel = 'None Recorded';
            } elseif ($brakeTestResult->generalPass) {
                $escLabel = 'Pass';
            } else {
                $escLabel = 'Fail';
            }

            echo $escLabel;
            ?>
        </p>
    </div>
    <span class="divider"></span>
</div>