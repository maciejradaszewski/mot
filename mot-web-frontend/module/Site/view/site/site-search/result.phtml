<?php

use Site\ViewModel\SiteSearchViewModel;

/**
 * @var SiteSearchViewModel $viewModel
 */

?>

<?php if ($viewModel->getTable()->getRowsTotalCount() !== 0): ?>

    <?php echo $this->partial('report/top-line', [
        'text'          => 'Showing <strong>%s</strong> sites found <strong>%s</strong>',
        'itemsNumber'   => $viewModel->getTable()->getRowsTotalCount(),
        'label'         => ''
    ]); ?>

    <?php echo $viewModel->getTable()->renderTable($this); ?>
    <?php echo $viewModel->getTable()->renderFooter($this); ?>

<?php endif; ?>

<nav class="content-navigation">
    <?php echo $this->partial(
        'partial/gds/form/control-navigation-secondary',
        [
            'navigation'    => [
                'Return to search' => $viewModel->getSearchPage(),
            ]
        ]
    ); ?>
</nav>
